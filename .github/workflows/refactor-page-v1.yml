name: Temporary page refactor v1

on:
  push:
    branches:
      - refactor/page-modularization-v1
    paths:
      - ".github/workflows/refactor-page-v1.yml"
      - "scripts/refactor-page-v1.mjs"

permissions:
  contents: write

jobs:
  refactor-and-validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout refactor branch
        uses: actions/checkout@v4
        with:
          ref: refactor/page-modularization-v1
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Capture baseline TypeScript debt
        shell: bash
        run: |
          set +e
          npx tsc --noEmit --pretty false > /tmp/tsc-before.txt 2>&1
          status=$?
          set -e
          echo "Baseline tsc exit code: $status"
          sed -E 's/\([0-9]+,[0-9]+\)/(LINE,COL)/g' /tmp/tsc-before.txt | sort > /tmp/tsc-before-normalized.txt
          cat /tmp/tsc-before.txt

      - name: Apply deterministic refactor
        run: node scripts/refactor-page-v1.mjs

      - name: Verify refactor adds no TypeScript errors
        shell: bash
        run: |
          set +e
          npx tsc --noEmit --pretty false > /tmp/tsc-after.txt 2>&1
          status=$?
          set -e
          echo "Refactored tsc exit code: $status"
          sed -E 's/\([0-9]+,[0-9]+\)/(LINE,COL)/g' /tmp/tsc-after.txt | sort > /tmp/tsc-after-normalized.txt
          if ! diff -u /tmp/tsc-before-normalized.txt /tmp/tsc-after-normalized.txt; then
            echo "A refatoracao alterou o conjunto de erros TypeScript e foi bloqueada."
            echo "--- TypeScript apos refatoracao ---"
            cat /tmp/tsc-after.txt
            exit 1
          fi
          echo "Nenhum erro TypeScript novo foi introduzido; a divida existente permaneceu equivalente."

      - name: Production build validation
        run: npm run build

      - name: Existing recovery tests
        run: npm run test:recovery

      - name: Diff hygiene
        run: git diff --check

      - name: Remove temporary refactor tooling
        run: |
          rm -f scripts/refactor-page-v1.mjs
          rm -f .github/workflows/refactor-page-v1.yml

      - name: Commit validated refactor
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "refactor: extrai helpers e branding do page"
          git push origin HEAD:refactor/page-modularization-v1
